---
import SetupHandshakeIsland from './SetupHandshakeIsland.tsx';

// The visual of Part 2 of s01-m01-seven-syscalls: socket, bind and listen,
// then the handshake that the kernel does before accept().
---

<figure class="s1m1-figure">
  <figcaption>Setup, then a client arrives</figcaption>
  <SetupHandshakeIsland client:visible />
</figure>
