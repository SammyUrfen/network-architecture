# Fixture curriculum

A small copy of the shapes in docs/curriculum, for the verifier tests.

| ID | The idea | First seen |
|---|---|---|
| `T-framing` | TCP gives a byte stream. Every protocol must say where a message ends. | S1 |
| `T-round-trip-tax` | Each round trip costs latency. | S2 |
| T-no-backticks | A row with no backticks is not a thread. | S1 |
